"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useQuery, useQueryClient } from "@tanstack/react-query"; import { api } from "../lib/api"; import { useAuth } from "../lib/auth";
type Organization={id:string;name:string;slug:string}; type Project={id:string;organization_id:string;name:string;key:string;description?:string};
export default function ProjectList({initialProjects}:{initialProjects?:Project[]}) { const token=useAuth((s)=>s.token); const client=useQueryClient(); const [error,setError]=useState(""); const orgs=useQuery({queryKey:["organizations",token],queryFn:()=>api<Organization[]>("/organizations",token),enabled:!!token}); const projects=useQuery({queryKey:["projects",token],queryFn:()=>api<Project[]>("/projects",token),enabled:!!token,initialData:initialProjects}); if(!token)return <p>Please <Link href="/login">log in</Link> to view projects.</p>; async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api("/projects",token,{method:"POST",body:JSON.stringify({organization_id:f.get("organization_id"),name:f.get("name"),key:f.get("key"),description:f.get("description")||undefined})});e.currentTarget.reset();client.invalidateQueries({queryKey:["projects",token]});}catch(e){setError(e instanceof Error?e.message:"Unable to create project");}} return <><h1>Projects</h1>{projects.data?.map(p=><Link className="card" href={`/projects/${p.id}`} key={p.id}><strong>{p.key} · {p.name}</strong><div className="muted">{p.description}</div></Link>)}{projects.isLoading&&<p>Loading projects…</p>}<h2>New project</h2>{orgs.data?.length===0?<p>Create an organization through the API first, then return here.</p>:<form onSubmit={submit}><select name="organization_id" required defaultValue=""><option value="" disabled>Organization</option>{orgs.data?.map(o=><option value={o.id} key={o.id}>{o.name}</option>)}</select><input name="name" placeholder="Project name" required/><input name="key" placeholder="Key, e.g. FORGE" required minLength={2} maxLength={20}/><textarea name="description" placeholder="Description"/><button>Create project</button>{error&&<p className="error">{error}</p>}</form>}</>; }
