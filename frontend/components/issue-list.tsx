"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useQuery, useQueryClient } from "@tanstack/react-query"; import { api } from "../lib/api"; import { useAuth } from "../lib/auth";
type Issue={id:string;title:string;description:string;status:string;priority:string};
export default function IssueList({projectId}:{projectId:string}) { const token=useAuth(s=>s.token); const client=useQueryClient();const [error,setError]=useState("");const issues=useQuery({queryKey:["issues",projectId,token],queryFn:()=>api<Issue[]>(`/projects/${projectId}/issues`,token),enabled:!!token});if(!token)return <p>Please <Link href="/login">log in</Link>.</p>;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api(`/projects/${projectId}/issues`,token,{method:"POST",body:JSON.stringify({title:f.get("title"),description:f.get("description"),priority:f.get("priority")})});e.currentTarget.reset();client.invalidateQueries({queryKey:["issues",projectId,token]});}catch(e){setError(e instanceof Error?e.message:"Unable to create issue");}}return <><Link href="/projects">← Projects</Link><h1>Issues</h1>{issues.data?.map(i=><Link className="card" href={`/issues/${i.id}`} key={i.id}><strong>{i.title}</strong><div className="muted">{i.status} · {i.priority}</div></Link>)}<h2>Create issue</h2><form onSubmit={submit}><input name="title" placeholder="Issue title" required/><textarea name="description" placeholder="Description" required/><select name="priority" defaultValue="medium"><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="urgent">Urgent</option></select><button>Create issue</button>{error&&<p className="error">{error}</p>}</form></>; }
