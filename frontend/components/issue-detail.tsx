"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useQuery, useQueryClient } from "@tanstack/react-query"; import { api } from "../lib/api"; import { useAuth } from "../lib/auth";
type Issue={id:string;project_id:string;title:string;description:string;status:string;priority:string};
export default function IssueDetail({issueId}:{issueId:string}) {const token=useAuth(s=>s.token),client=useQueryClient(),[error,setError]=useState("");const issue=useQuery({queryKey:["issue",issueId,token],queryFn:()=>api<Issue>(`/issues/${issueId}`,token),enabled:!!token});if(!token)return <p>Please <Link href="/login">log in</Link>.</p>;if(issue.isLoading)return <p>Loading issue…</p>;if(!issue.data)return <p>Issue not found.</p>;async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api(`/issues/${issueId}`,token,{method:"PATCH",body:JSON.stringify({title:f.get("title"),description:f.get("description"),status:f.get("status"),priority:f.get("priority")})});client.invalidateQueries({queryKey:["issue",issueId,token]});}catch(e){setError(e instanceof Error?e.message:"Unable to update issue");}} const i=issue.data;return <><Link href={`/projects/${i.project_id}`}>← Issues</Link><h1>{i.title}</h1><form onSubmit={submit}><input name="title" defaultValue={i.title} required/><textarea name="description" defaultValue={i.description} required/><select name="status" defaultValue={i.status}><option value="open">Open</option><option value="in_progress">In progress</option><option value="closed">Closed</option></select><select name="priority" defaultValue={i.priority}><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="urgent">Urgent</option></select><button>Save changes</button>{error&&<p className="error">{error}</p>}</form></>; }
